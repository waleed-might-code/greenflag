# Security Assessment - Executive Summary

**Target:** {{ target_url }}
**Assessment Date:** {{ generated_at[:10] }}
**Run ID:** {{ run_id }}

## Overview

This automated security assessment identified **{{ findings | length }}** verified vulnerabilities with full evidence traceability through WARC archives.

## Risk Summary

| Severity | Count | Evidence Captures |
|----------|-------|-------------------|
| Critical | {{ severity_breakdown.get('critical', 0) }} | {{ warc_refs_by_severity.get('critical', 0) }} WARC refs |
| High | {{ severity_breakdown.get('high', 0) }} | {{ warc_refs_by_severity.get('high', 0) }} WARC refs |
| Medium | {{ severity_breakdown.get('medium', 0) }} | {{ warc_refs_by_severity.get('medium', 0) }} WARC refs |
| Low | {{ severity_breakdown.get('low', 0) }} | {{ warc_refs_by_severity.get('low', 0) }} WARC refs |
| Info | {{ severity_breakdown.get('info', 0) }} | {{ warc_refs_by_severity.get('info', 0) }} WARC refs |

**Total Evidence Artifacts:** {{ total_warc_refs }} HTTP request/response pairs archived

## Top Priority Issues

{% for finding in findings[:5] %}
### {{ loop.index }}. {{ finding.title }}

- **Severity:** {{ finding.severity.value | upper }}
- **Asset:** `{{ finding.asset }}`
- **Impact:** {{ finding.business_impact or finding.confirmed_impact or "Potential security compromise" }}
- **Evidence:** {{ finding.evidence_warc_refs | length }} WARC-backed HTTP captures
- **Reliability:** {{ "%.0f" | format(finding.reliability_score * 100) }}%

{% endfor %}

## Evidence Traceability

All findings are backed by immutable WARC archives following ISO 28500 standard:
- **Format:** WARC 1.0 (Web ARChive)
- **Storage:** Content-addressed with SHA-256 IDs
- **Retention:** Per engagement data retention policy
- **Accessibility:** Evidence packs available for each finding

## Recommendations

1. **Immediate Action Required:** Address all Critical and High severity findings
2. **Short-term:** Review and remediate Medium severity findings  
3. **Evidence Review:** Download evidence packs for detailed HTTP captures
4. **Ongoing:** Implement security testing in CI/CD pipeline

## Next Steps

1. Review detailed findings in the Engineering Handoff document
2. Download evidence packs for reproduction and verification
3. Prioritize remediation based on business impact and reliability scores
4. Schedule verification testing after fixes are deployed

---
*Generated by CyberAI ASRTS Platform - Evidence-Grade Security Testing*

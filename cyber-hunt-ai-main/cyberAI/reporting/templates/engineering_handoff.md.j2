# Engineering Handoff - Security Findings

**Target:** {{ target_url }}
**Run ID:** {{ run_id }}
**Generated:** {{ generated_at }}

## Summary

This document contains detailed technical information for remediating {{ findings | length }} security findings. Each finding includes WARC-backed evidence for full reproducibility.

---

{% for finding in findings %}
## Finding {{ loop.index }}: {{ finding.title }}

**ID:** `{{ finding.id }}`
**Severity:** {{ finding.severity.value | upper }}
**Category:** {{ finding.category.value }}
**Reliability Score:** {{ "%.2f" | format(finding.reliability_score) }}/1.0

### Affected Asset

```
{{ finding.asset }}
```

### Root Cause

{{ finding.root_cause or "Insufficient security controls" }}

### Technical Impact

{{ finding.technical_impact or "Security control bypass" }}

### Business Impact

{{ finding.business_impact or "Potential data exposure or unauthorized access" }}

### Reproduction Steps

{% for step in finding.reproduction_steps %}
{{ step.step_number }}. {{ step.action }}
   {% if step.expected_result %}
   - Expected: {{ step.expected_result }}
   {% endif %}
   {% if step.actual_result %}
   - Actual: {{ step.actual_result }}
   {% endif %}
{% endfor %}

### Evidence Artifacts

{% if finding.evidence_warc_refs %}
**WARC References ({{ finding.evidence_warc_refs | length }} HTTP captures):**

{% for ref in finding.evidence_warc_refs %}
{{ loop.index }}. `{{ ref }}`
{% endfor %}

**Evidence Pack:** `evidence_{{ finding.id }}.zip`
- Contains full HTTP request/response pairs extracted from WARC
- Includes screenshots and reproduction guide
- Available in `reports/evidence_packs/` directory
{% else %}
No WARC evidence available for this finding.
{% endif %}

### Fix Recommendation

{% if finding.root_cause %}
**Root Cause:** {{ finding.root_cause }}

**Remediation Steps:**
{% endif %}
- Implement proper authorization checks before resource access
- Validate all user input against expected schemas
- Add rate limiting for sensitive operations
- Implement audit logging for security-relevant actions

### CWE Mapping

{% if finding.cwe_ids %}
{% for cwe in finding.cwe_ids %}
- {{ cwe }}
{% endfor %}
{% else %}
- Not mapped
{% endif %}

### OWASP Categories

{% if finding.owasp_categories %}
{% for cat in finding.owasp_categories %}
- {{ cat }}
{% endfor %}
{% else %}
- Not mapped
{% endif %}

### Verification

After fixing, verify by:
1. Re-running the test case with the same reproduction steps
2. Confirming expected error responses (401/403 for authz issues)
3. Testing with multiple user roles (if applicable)
4. Validating that WARC evidence shows corrected behavior

---

{% endfor %}

## Remediation Priority Queue

{% if remediation_queue %}
{% for item in remediation_queue %}
### Priority {{ item.priority }}: {{ item.fix_description }}

- **Affected Findings:** {{ item.finding_ids | length }}
- **Estimated Effort:** {{ item.estimated_effort }}
- **Complexity:** {{ item.complexity }}

{% endfor %}
{% endif %}

## Security Checklist

- [ ] All Critical findings addressed
- [ ] All High findings addressed
- [ ] Code review completed
- [ ] Security tests added/updated
- [ ] Verification testing passed
- [ ] Evidence packs reviewed

## Evidence Archive Information

All HTTP traffic is archived in WARC format (ISO 28500) with:
- Content-addressed IDs for deduplication
- Immutable storage for audit trail
- Fast O(1) lookup via index
- Retention per engagement policy

---
*Generated by CyberAI ASRTS Platform - Evidence-Grade Security Testing*
